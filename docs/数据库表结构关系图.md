# SIGS 接诉即办数据库表结构与关系图

本文档根据 `server/db_mysql.js` 中的 MySQL 初始化逻辑和 `server/index.js` 中的主要业务查询整理。

说明：

- 当前数据库主要使用 MySQL/InnoDB，字符集为 `utf8mb4`。
- 代码里大部分关系通过应用层字段维护，例如 `ticket_id`、`person_id`、`department_name`、`user_id`。
- 表结构中多数未显式声明 `FOREIGN KEY`，下图表达的是业务关系和联表关系，不代表数据库强约束全部存在。
- `submitter_id`、`user_id`、`operator_id`、`replier_id` 等字段历史上兼容本地账号 `users.id` 与统一人员表 `datahub_basic_persons.id`，因此在图中按“可能关联”表达。

## 1. 业务域分层架构图

```mermaid
flowchart TB
  subgraph Identity["人员与权限域"]
    users["users\n本地账号"]
    persons["datahub_basic_persons\n统一人员/Datahub 同步人员"]
    sync_runs["datahub_basic_person_sync_runs\n人员同步批次"]
    roles["roles\n角色"]
    permissions["permissions\n权限点"]
    role_permissions["role_permissions\n角色-权限"]
    dept_assignments["department_assignments\n部门管理员授权"]
    dept_leaders["department_leaders\n部门领导配置"]
    audit_log["permission_audit_log\n权限审计日志"]
  end

  subgraph Config["配置域"]
    departments["departments\n部门配置"]
    form_options["form_options\n表单选项/事项领域"]
  end

  subgraph Ticket["事项工单域"]
    tickets["tickets\n事项主表"]
    replies["replies\n部门回复"]
    attachments["attachments\n用户/回复附件"]
    transfers["transfers\n转办记录"]
    followups["ticket_followups\n提交人补充说明"]
    approvals["ticket_approvals\n领导审批记录"]
  end

  subgraph Interaction["通知与反馈域"]
    notifications["notifications\n站内通知"]
    satisfaction["satisfaction_surveys\n满意度评价"]
    ratings["ratings\n点赞/点踩/收藏"]
  end

  persons --> sync_runs
  persons --> roles
  roles --> role_permissions
  permissions --> role_permissions
  persons --> dept_assignments
  persons --> dept_leaders
  departments --> dept_assignments
  departments --> dept_leaders
  departments --> tickets
  form_options --> tickets
  tickets --> replies
  tickets --> attachments
  replies --> attachments
  tickets --> transfers
  tickets --> followups
  tickets --> approvals
  tickets --> notifications
  tickets --> satisfaction
  tickets --> ratings
  persons --> notifications
  users --> notifications
  persons --> satisfaction
  users --> satisfaction
  persons --> ratings
  users --> ratings
  dept_assignments --> audit_log
  dept_leaders --> approvals
```

## 2. 核心关系图 ERD

```mermaid
erDiagram
  users {
    BIGINT id PK
    VARCHAR username UK
    VARCHAR password
    VARCHAR password_hash
    VARCHAR name
    VARCHAR phone
    VARCHAR department
    VARCHAR role
    VARCHAR union_id
    TINYINT is_active
    TINYINT must_change_password
    TIMESTAMP created_at
  }

  datahub_basic_persons {
    VARCHAR id PK
    VARCHAR person_id
    VARCHAR union_id
    VARCHAR username UK
    VARCHAR name
    VARCHAR type
    VARCHAR category
    VARCHAR department
    VARCHAR status
    VARCHAR phone
    VARCHAR role
    INT role_id
    TINYINT can_manage_roles
    VARCHAR auth_source
    TINYINT is_active
    TEXT raw_json
    TIMESTAMP synced_at
    TIMESTAMP created_at
    TIMESTAMP updated_at
  }

  datahub_basic_person_sync_runs {
    BIGINT id PK
    TIMESTAMP started_at
    TIMESTAMP finished_at
    VARCHAR start_date
    INT page_size
    INT fetched_count
    INT upserted_count
    VARCHAR status
    TEXT error_message
  }

  roles {
    INT id PK
    VARCHAR code UK
    VARCHAR name
    TINYINT is_system
    DATETIME created_at
  }

  permissions {
    INT id PK
    VARCHAR code UK
    VARCHAR name
    VARCHAR module
    DATETIME created_at
  }

  role_permissions {
    INT id PK
    INT role_id
    INT permission_id
  }

  departments {
    BIGINT id PK
    VARCHAR name UK
    VARCHAR name_en
    VARCHAR type
    INT sort_order
    TINYINT is_active
    TIMESTAMP created_at
    TIMESTAMP updated_at
  }

  form_options {
    BIGINT id PK
    VARCHAR category
    VARCHAR label
    VARCHAR label_en
    INT sort_order
    TINYINT is_active
    TIMESTAMP created_at
    TIMESTAMP updated_at
  }

  tickets {
    BIGINT id PK
    VARCHAR ticket_code UK
    VARCHAR share_code
    VARCHAR title
    VARCHAR field
    VARCHAR unit_type
    VARCHAR department
    VARCHAR original_department
    VARCHAR current_department
    TEXT content
    TINYINT is_anonymous
    VARCHAR submitter_id
    VARCHAR submitter_union_id
    VARCHAR submitter_person_id
    VARCHAR submitter_name
    VARCHAR submitter_phone
    VARCHAR submitter_role
    VARCHAR submitter_department
    VARCHAR current_handler_id
    VARCHAR status
    VARCHAR resolution_status
    TIMESTAMP resolution_confirmed_at
    TINYINT is_published
    TIMESTAMP published_at
    VARCHAR ai_category
    TEXT ai_suggestion
    TIMESTAMP created_at
    TIMESTAMP updated_at
  }

  replies {
    BIGINT id PK
    BIGINT ticket_id
    TEXT content
    VARCHAR replier_id
    VARCHAR department
    TIMESTAMP created_at
  }

  attachments {
    BIGINT id PK
    BIGINT ticket_id
    BIGINT reply_id
    VARCHAR filename
    VARCHAR original_name
    VARCHAR file_path
    BIGINT file_size
    VARCHAR file_type
    TIMESTAMP uploaded_at
  }

  transfers {
    BIGINT id PK
    BIGINT ticket_id
    VARCHAR from_department
    VARCHAR to_department
    VARCHAR operator_id
    VARCHAR from_handler_id
    VARCHAR to_handler_id
    TEXT note
    VARCHAR status
    TIMESTAMP created_at
  }

  ticket_followups {
    BIGINT id PK
    BIGINT ticket_id
    TEXT content
    VARCHAR submitter_id
    TIMESTAMP created_at
  }

  notifications {
    BIGINT id PK
    VARCHAR user_id
    BIGINT ticket_id
    VARCHAR type
    VARCHAR message
    VARCHAR target_url
    TINYINT is_read
    TIMESTAMP created_at
  }

  satisfaction_surveys {
    BIGINT id PK
    BIGINT ticket_id UK
    VARCHAR user_id
    INT score
    TEXT comment
    TIMESTAMP created_at
    TIMESTAMP updated_at
  }

  ratings {
    BIGINT id PK
    BIGINT ticket_id
    VARCHAR user_id
    VARCHAR type
    TIMESTAMP created_at
  }

  department_assignments {
    INT id PK
    VARCHAR person_id
    VARCHAR department_name
    VARCHAR role_type
    TEXT can_transfer_to
    TINYINT is_enabled
    DATETIME created_at
    DATETIME updated_at
  }

  department_leaders {
    INT id PK
    VARCHAR person_id
    VARCHAR department_name
    TINYINT is_enabled
    VARCHAR created_by
    DATETIME created_at
    DATETIME updated_at
  }

  ticket_approvals {
    BIGINT id PK
    BIGINT ticket_id
    VARCHAR department_name
    VARCHAR requested_by
    TEXT request_note
    VARCHAR status
    VARCHAR approver_id
    TEXT decision_comment
    TIMESTAMP requested_at
    TIMESTAMP decided_at
  }

  permission_audit_log {
    BIGINT id PK
    VARCHAR operator_id
    VARCHAR target_person_id
    VARCHAR action
    TEXT before_json
    TEXT after_json
    TIMESTAMP created_at
  }

  roles ||--o{ role_permissions : role_id
  permissions ||--o{ role_permissions : permission_id
  roles ||--o{ datahub_basic_persons : role_id

  departments ||--o{ department_assignments : department_name
  datahub_basic_persons ||--o{ department_assignments : person_id
  departments ||--o{ department_leaders : department_name
  datahub_basic_persons ||--o{ department_leaders : person_id

  departments ||--o{ tickets : current_department
  form_options ||--o{ tickets : field
  tickets ||--o{ replies : ticket_id
  tickets ||--o{ attachments : ticket_id
  replies ||--o{ attachments : reply_id
  tickets ||--o{ transfers : ticket_id
  tickets ||--o{ ticket_followups : ticket_id
  tickets ||--o{ notifications : ticket_id
  tickets ||--o{ satisfaction_surveys : ticket_id
  tickets ||--o{ ratings : ticket_id
  tickets ||--o{ ticket_approvals : ticket_id

  datahub_basic_persons ||--o{ ticket_approvals : requested_by
  datahub_basic_persons ||--o{ ticket_approvals : approver_id
  datahub_basic_persons ||--o{ permission_audit_log : target_person_id
```

## 3. 主要业务流程与数据流

### 3.1 提交事项

```mermaid
sequenceDiagram
  participant User as 普通用户
  participant App as 前端/接口
  participant Ticket as tickets
  participant Attach as attachments
  participant Notice as notifications/统一待办

  User->>App: 填写标题、领域、部门、内容、附件
  App->>Ticket: INSERT tickets
  App->>Attach: INSERT attachments(ticket_id)
  App->>Notice: 通知当前承办部门管理员
```

相关表：

| 表 | 作用 |
| --- | --- |
| `tickets` | 事项主记录，保存标题、领域、部门、提交人快照、状态 |
| `attachments` | 用户提交附件，`ticket_id` 指向事项 |
| `notifications` | 给部门管理员、提交人、领导发送站内通知 |
| `departments` | 提交表单中的部门来源 |
| `form_options` | 提交表单中的事项领域来源 |

### 3.2 部门处理、回复与转办

```mermaid
sequenceDiagram
  participant Admin as 部门管理员
  participant Ticket as tickets
  participant Reply as replies
  participant Attach as attachments
  participant Transfer as transfers
  participant Notice as notifications/统一待办

  Admin->>Ticket: 查看当前部门待处理事项
  alt 直接回复
    Admin->>Reply: INSERT replies(ticket_id)
    Admin->>Attach: INSERT attachments(reply_id)
    Admin->>Ticket: UPDATE status/resolution_status
    Admin->>Notice: 通知提交人确认结果
  else 转办
    Admin->>Transfer: INSERT transfers(ticket_id)
    Admin->>Ticket: UPDATE current_department
    Admin->>Notice: 通知目标部门管理员
  end
```

相关表：

| 表 | 作用 |
| --- | --- |
| `replies` | 部门管理员处理回复 |
| `attachments` | 回复附件，`reply_id` 指向回复记录 |
| `transfers` | 转办历史，当前有效记录 `status='active'` |
| `tickets.current_department` | 当前承办部门 |
| `department_assignments` | 判断哪些人员有部门管理员权限 |

### 3.3 领导审批

```mermaid
sequenceDiagram
  participant Admin as 部门管理员
  participant Leader as 部门领导
  participant Approval as ticket_approvals
  participant Notice as notifications/统一待办
  participant Ticket as tickets

  Admin->>Approval: INSERT ticket_approvals(status=pending)
  Admin->>Notice: 通知该部门领导
  Leader->>Approval: UPDATE status=approved/rejected
  Approval->>Notice: 通知发起审批的部门管理员
  Admin->>Ticket: 审批完成后继续回复处理
```

相关表：

| 表 | 作用 |
| --- | --- |
| `department_leaders` | 维护部门领导和部门的对应关系 |
| `ticket_approvals` | 记录一次领导审批请求、审批人、审批意见 |
| `notifications` | 领导待审批通知、审批结果通知 |
| `tickets` | 审批期间保持待处理状态，审批完成后由管理员继续处理 |

### 3.4 满意度与互动

```mermaid
sequenceDiagram
  participant User as 提交人
  participant Ticket as tickets
  participant Followup as ticket_followups
  participant Survey as satisfaction_surveys
  participant Rating as ratings

  alt 问题未解决
    User->>Followup: INSERT ticket_followups
    User->>Ticket: UPDATE status=pending
  else 问题已解决
    User->>Ticket: UPDATE resolution_status=resolved
    User->>Survey: INSERT/UPDATE satisfaction_surveys
  end
  User->>Rating: 点赞/点踩/收藏
```

相关表：

| 表 | 作用 |
| --- | --- |
| `ticket_followups` | 提交人补充说明，工单重新进入待处理 |
| `satisfaction_surveys` | 满意度评分与评价，一张工单唯一一条评价 |
| `ratings` | 点赞、点踩、收藏等用户互动 |

## 4. 表清单与字段重点

| 业务域 | 表 | 主键 | 关键字段 | 说明 |
| --- | --- | --- | --- | --- |
| 人员账号 | `users` | `id` | `username`, `union_id`, `role`, `department` | 本地账号与兼容账号 |
| 人员账号 | `datahub_basic_persons` | `id` | `union_id`, `name`, `department`, `role_id`, `is_active` | 统一人员库同步数据，也是权限授权主要人员来源 |
| 人员账号 | `datahub_basic_person_sync_runs` | `id` | `status`, `fetched_count`, `upserted_count` | Datahub 同步批次记录 |
| RBAC | `roles` | `id` | `code`, `name` | 系统角色 |
| RBAC | `permissions` | `id` | `code`, `module` | 权限点 |
| RBAC | `role_permissions` | `id` | `role_id`, `permission_id` | 角色权限关联 |
| 配置 | `departments` | `id` | `name`, `name_en`, `type`, `is_active` | 部门配置 |
| 配置 | `form_options` | `id` | `category`, `label`, `label_en`, `is_active` | 事项领域等表单选项 |
| 工单 | `tickets` | `id` | `ticket_code`, `current_department`, `status`, `submitter_id` | 事项主表 |
| 工单 | `replies` | `id` | `ticket_id`, `replier_id`, `department` | 部门处理回复 |
| 工单 | `attachments` | `id` | `ticket_id`, `reply_id`, `original_name`, `file_path` | 用户附件和回复附件 |
| 工单 | `transfers` | `id` | `ticket_id`, `from_department`, `to_department`, `status` | 转办记录 |
| 工单 | `ticket_followups` | `id` | `ticket_id`, `submitter_id` | 用户补充说明 |
| 通知 | `notifications` | `id` | `user_id`, `ticket_id`, `type`, `target_url`, `is_read` | 站内通知 |
| 反馈 | `satisfaction_surveys` | `id` | `ticket_id`, `user_id`, `score` | 满意度评价 |
| 反馈 | `ratings` | `id` | `ticket_id`, `user_id`, `type` | 点赞、点踩、收藏 |
| 授权 | `department_assignments` | `id` | `person_id`, `department_name`, `role_type`, `can_transfer_to` | 部门管理员授权 |
| 授权 | `department_leaders` | `id` | `person_id`, `department_name`, `is_enabled` | 部门领导配置 |
| 授权 | `permission_audit_log` | `id` | `operator_id`, `target_person_id`, `action` | 授权变更审计 |
| 审批 | `ticket_approvals` | `id` | `ticket_id`, `department_name`, `status`, `approver_id` | 领导审批记录 |

## 5. 关键索引与唯一约束

| 表 | 约束/索引 | 作用 |
| --- | --- | --- |
| `users` | `username UNIQUE` | 本地账号唯一 |
| `tickets` | `idx_tickets_submitter_id`, `uniq_tickets_ticket_code` | 按提交人查询、按工单编号访问 |
| `replies` | `idx_replies_ticket_id`, `idx_replies_replier_id` | 按工单查回复、按回复人查记录 |
| `attachments` | `idx_attachments_ticket_id`, `idx_attachments_reply_id` | 区分用户附件和回复附件 |
| `transfers` | `idx_transfers_ticket_id`, `idx_transfers_operator_id` | 查工单转办链路 |
| `notifications` | `idx_notifications_ticket_id` | 查工单关联通知 |
| `satisfaction_surveys` | `uniq_satisfaction_ticket` | 一张工单一条满意度 |
| `ratings` | `uniq_rating_ticket_user_type` | 同一用户对同一工单同一互动类型唯一 |
| `roles` | `code UNIQUE` | 角色编码唯一 |
| `permissions` | `code UNIQUE` | 权限编码唯一 |
| `role_permissions` | `uniq_role_permission` | 避免重复授权 |
| `datahub_basic_persons` | `username UNIQUE`, `idx_datahub_basic_persons_union_id`, `idx_datahub_basic_persons_department` | 人员查找、部门授权检索 |
| `form_options` | `uniq_category_label`, `idx_form_options_category`, `idx_form_options_active` | 表单配置查询 |
| `departments` | `name UNIQUE` | 部门中文名唯一 |
| `department_assignments` | `uniq_person_department`, `idx_department_assignments_person_id`, `idx_department_assignments_department_name` | 人员-部门授权唯一 |
| `department_leaders` | `uniq_department_leaders_person_department`, `idx_department_leaders_person_id`, `idx_department_leaders_department_name` | 部门领导配置唯一 |
| `ticket_approvals` | `idx_ticket_approvals_ticket_id`, `idx_ticket_approvals_status` | 审批列表和审批状态查询 |

## 6. 关系注意事项

1. `tickets.submitter_id`、`notifications.user_id`、`ratings.user_id`、`satisfaction_surveys.user_id` 是字符串字段，历史上兼容本地 `users.id` 与统一人员 `datahub_basic_persons.id`。
2. `department_name` 和 `current_department` 是按部门中文名关联 `departments.name`，不是按部门 `id` 关联。
3. `attachments` 同时支持两类附件：
   - 用户提交附件：`ticket_id` 有值，`reply_id` 为空。
   - 部门回复附件：`reply_id` 有值，可通过 `replies.ticket_id` 回到事项。
4. `ticket_approvals` 不直接改变 `tickets.status` 为审批状态，审批中状态主要通过存在 `pending` 审批记录判断。
5. `permission_audit_log` 记录授权变更前后 JSON 快照，不参与业务强约束。
6. 统一待办不落独立业务表，主要由代码调用外部接口推送；本系统内部只保存 `notifications`。
